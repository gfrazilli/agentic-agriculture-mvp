# V2 capture source audit — Scenes 8–14

Audit date: 2026-08-31 (America/Sao_Paulo)

## Final assembly note

This report was the pre-assembly source audit. The deadline V2 intentionally uses the documented
real screenshots as editorial pickups instead of claiming that they are uninterrupted screen
recordings. The final narration and cards contain no `continuous live run`, `no cuts`, or
`uninterrupted action` claim. Scene 11 presents the approved natural question and the answer path
grounded in the same persisted evidence; Scene 12 presents the real application state and its tool
trace; Scene 14 combines a sanitized current Cloud Run overview with separately verified, read-only
Cloud Logging records stored in `cloud_logging_evidence.txt`. These are truthful production-proof
summaries and the final fallback selected for the submission deadline.

Scope: all reusable media under
`C:\Users\giova\Videos\1415-Agri-Hackathon` plus the production documentation and assets under
`docs/submission/video/`. This report covers only V2 Scenes 8–14: the real workflow, result, Gemini
answer, state-changing action, feedback, and Google Cloud proof.

## Executive verdict

The existing material contains strong, internally consistent evidence from a real run:

- the field form, boundary proposal, processing checkpoints, result, five Sentinel-2 dates, spectral
  indices and provenance were captured in chronological order;
- the accepted Gemini response identifies Area 1, cites the persisted dates and values, and exposes
  Gemini 3.5 Flash, Google ADK, the temporal specialist, and evidence tools;
- the agent-action evidence shows `request_field_analysis`, a new analysis ID, three requested areas,
  queued/running state, and idempotent replay language;
- the feedback confirmation is visibly persisted;
- Cloud Run evidence shows all four runtime roles.

However, there is **no continuous workflow master, uninterrupted Gemini-action master, or sanitized
Cloud Console master** in the video workspace. The current `raw/scene_*.mp4` files were generated by
`scripts/video/create_raw_scenes.py` from PNG screenshots and editorial SVG cards. They are truthful
visual summaries, but they are not live screen recordings. Therefore:

- V2 Scenes **8–9 must be recaptured** as one native-speed continuous master, with Scene 10 derived
  from its completed tail;
- V2 Scenes **11–12 must be recaptured** to show the approved simple question and uninterrupted
  state mutation;
- Scene **13 should be recaptured** with the new conversation, although the existing before/after
  proof is an acceptable emergency fallback;
- Scene **14 must be recaptured** because the existing Console images are low resolution, in
  Portuguese, expose the signed-in account avatar, contain no real log view, and predate the current
  deployment.

The existing assets should be retained as factual pickups and deadline contingencies. They must not
be labeled `CONTINUOUS LIVE RUN` or `LIVE AGENT ACTION · UNINTERRUPTED`.

## Media inventory

All listed raw scene files are H.264, 1920×1080, 30 fps, and contain no audio. Their immediate source
is the still-image shot map in `scripts/video/create_raw_scenes.py` (lines 296–375), not a screen
recorder.

| Existing file | Duration | V2 use | Source and limitation |
| --- | ---: | --- | --- |
| `raw/scene_03_field_context.mp4` | 21.0 s | Scene 8 fallback | Editorial field card plus one real UI screenshot; the screenshot does not show all form values or the Guided Demo checkbox together. |
| `raw/scene_04_boundary.mp4` | 20.0 s | Scene 8 fallback | Old real boundary screenshot plus editorial confirmation card; the old screenshot contains the pre-fix mass of vertex handles and no live click/drag. |
| `raw/scene_05_processing.mp4` | 40.0 s | Scene 9 fallback | Six chronological screenshots from a measured live run; it does not show continuous elapsed time, cursor interaction, or a legible analysis ID. |
| `raw/scene_06_results.mp4` | 27.0 s | Scene 10 strong pickup | Three real result screenshots covering the four-area map, five dates, NDVI/NDRE/NDMI, cloud cover, source bands and processing provenance. It is not the uncut tail of the workflow. |
| `raw/scene_07_gemini.mp4` | 31.0 s | Scene 11 fallback | Real accepted response and trace assembled from screenshots. The recorded prompt is longer and more engineered than the approved V2 question, and no typing/response generation is continuous. |
| `raw/scene_08_agent_action.mp4` | 22.0 s | Scene 12 strong fallback | Real prompt/thinking/response screenshots. It visibly proves `request_field_analysis` and the returned analysis ID, but the video is a four-still montage, not an uninterrupted action. |
| `raw/scene_09_feedback.mp4` | 12.0 s | Scene 13 usable fallback | Real before/after screenshots; the confirmation `Thank you. Your feedback was recorded.` is clear. It belongs to the prior conversation. |
| `raw/scene_10_cloud.mp4` | 17.0 s | Scene 14 supporting only | A cropped Console screenshot followed by an editorial verification card. It does not show an actual matching log in Console and reflects older revisions/image. |

The corresponding narrated/captioned V1 exports in `edit/build/segment-03-...` through
`segment-10-...` have the same durations and 1080p/30 fps format, but contain the superseded V1
narration and captions. Reuse the audio-free `raw/` files or original PNGs instead.

The old final master, `final/1415-agri-hackathon.mp4`, is 233.0 seconds, 1920×1080, 30 fps, H.264/AAC.
It is useful only as a timing and visual reference; it should not be cut into the V2 because captions
and narration are already burned in.

## Original screenshot evidence

The application screenshots are 1905×974 and their filesystem times preserve a credible chronological
sequence:

| Group | Files | Capture interval | What is reusable |
| --- | --- | --- | --- |
| Field | `captures/scene03_field_filled.png`, `scene03_field_units_36ha.png` | 10:41:53–10:42:38 | Field name and Soybeans UI; precise coordinates are not visible in the used crop. |
| Boundary | `captures/scene04_boundary_*.png` | 10:42:52–10:44:20 | Real 30.3 ha / 74% proposal and explicit confirmation state. Use only as documentary fallback because the UI predates the vertex-visibility fix. |
| Processing | `captures/scene05_processing_00.png`, `_08.png`, `_25.png`, `_46.png`, `_63.png`, `_82.png` | 10:44:37–10:46:09 | Real chronological 0% through completion checkpoints, consistent with the measured 93-second run. |
| Result | `captures/scene06_result_top.png`, `_areas.png`, `_evidence.png` | 10:46:32–10:46:58 | Best existing pickups for four areas, five observations, dates, indices, cloud cover, bands, source and processing version. |
| Gemini | `captures/scene07_*.png` | 10:47:33–10:54:15 | Accepted Area 1 response, February 22 / March 14 evidence, three field checks, Gemini 3.5 Flash, ADK, specialist, and tools. Several screenshots represent retries/scroll positions rather than one continuous take. |
| Agent action | `captures/scene08_action_prompt.png`, `_thinking.png`, `_response_22.png` | 10:54:44–10:55:19 | Strong proof of the action result and tool trace. The approved V2 wording is not the wording visible here. |
| Feedback | `captures/scene09_feedback_before.png`, `_after.png` | 10:55:41–10:55:42 | Clear, real before/after confirmation. |

The Cloud Console screenshots are only 960×698:

- `captures/scene10_cloud_services.png` shows the four named Cloud Run services, but the Console is in
  Portuguese and the account avatar is visible;
- `captures/scene10_cloud_console.png` includes a promotional pop-up and does not expose a matching
  worker log;
- neither image should appear uncropped in a public submission.

The SVG assets under `docs/submission/video/assets/` are 1920×1080 and remain useful as supporting
callouts:

- `scene_03_field_context.svg` — sanitized field facts;
- `scene_04_boundary_control.svg` — 36 ha estimate, 30.3 ha proposal, 74% confidence, human
  confirmation;
- `callout_03_deterministic_pixels.svg` — deterministic processing explanation;
- `callout_04_gemini_adk_mcp.svg` — Gemini/ADK/MCP trace explanation;
- `callout_05_agent_action.svg` — agent mutation explanation;
- `callout_06_cloud_roles.svg` — four Cloud Run roles;
- `scene_10_cloud_verified.svg` — old revision/digest and the completed prior action analysis.

`scene_10_cloud_verified.svg` is a factual editorial summary, not independent Console proof. It
references the prior deployment (`app:c27da853f549`, revisions 00029/00016). The current production
deployment uses newer revisions and must be shown in the new Console capture.

## Continuous-capture finding

None of the capture masters required by `docs/submission/video/shot_list.md` exists anywhere under the
video workspace:

- `02_live_workflow_master.mp4` — absent;
- `03_gemini_grounded_master.mp4` — absent;
- `04_agent_action_master.mp4` — absent;
- `05_feedback_master.mp4` — absent;
- `06_cloud_proof_master.mp4` — absent;
- the three optional pickup masters are also absent.

The only recorder-origin MP4s are `raw/capture-test*.mp4`, each only 2–3 seconds. The one containing a
visible desktop has unrelated tabs, a browser debug banner, and a Chrome tab-group pop-up. These files
are capture-device tests and are not reusable.

This distinction matters for the approved V2 language. The existing checkpoint montage may truthfully
say `CHECKPOINTS FROM THE MEASURED LIVE RUN`; it may not truthfully say `2× PLAYBACK · NO CUTS`.

## Scene-by-scene decision

### Scene 8 — real field and confirmed boundary: recapture required (P0)

Useful sources:

- `raw/scene_03_field_context.mp4`;
- `raw/scene_04_boundary.mp4`;
- `captures/scene03_*` and `captures/scene04_*`;
- `assets/scene_03_field_context.svg` and `assets/scene_04_boundary_control.svg`.

Why recapture:

- V2 promises the start of a continuous browser recording;
- the current form capture does not visibly prove all inputs plus Guided Demo off;
- the current boundary image predates the reduced-handle UI fix and is visually distracting;
- there is no real vertex drag, confirmation click, or continuous handoff into processing.

Capture Scene 8 as the opening portion of one native-speed master. Show the unchecked Guided Demo
control, keep precise coordinates out of frame, make one small honest boundary adjustment, and confirm.

### Scene 9 — asynchronous satellite workflow: recapture required (P0)

Useful sources:

- all six `captures/scene05_processing_*.png` checkpoints;
- `raw/scene_05_processing.mp4` as an emergency factual montage.

Why recapture:

- the six screenshots prove chronology but not continuous execution;
- the analysis ID is not legible in the captured progress UI;
- V2 explicitly labels this segment as a uniform 2× playback with no cuts.

Keep recording from Scene 8 through completion at native speed. Apply one uniform 2× speed change only
in the derived Scene 8–10 edit, preserve the untouched master, and disclose the speed-up on screen.

### Scene 10 — season becomes a priority: derive from the same master (P0 continuity; strong fallback exists)

Useful sources:

- `captures/scene06_result_top.png`;
- `captures/scene06_result_areas.png`;
- `captures/scene06_result_evidence.png`;
- `raw/scene_06_results.mp4`.

These are the strongest reusable sources in the audit and can rescue the result explanation if a live
scroll becomes illegible. For the primary V2, continue the same Scene 8–9 master through the completed
result, then scroll slowly enough to show dates, NDVI/NDRE/NDMI, cloud cover, bands, and provenance.

### Scene 11 — farmer question: recapture required (P0)

Useful sources:

- `captures/scene07_gemini_answer_evidence2.png` for the Area 1 values and trace;
- `captures/scene07_chat_focus.png` and `scene07_chat_lower_page.png` for the three field checks;
- `raw/scene_07_gemini.mp4` as a fallback visual.

Why recapture:

- the stored prompt is a long instruction that tells Gemini which area and response shape to use;
- the approved V2 question is the much stronger, natural farmer question: `Why should I check Area 1
  first, and what should I look for when I get there?`;
- the current scene is a screenshot montage, so the response and trace do not visibly emerge from that
  natural question.

Record the exact V2 question, hold the real answer at readable scroll positions, then open and hold the
real technology trace. The existing accepted response is a content benchmark: Area 1, both real dates,
all three indices, and three practical checks.

### Scene 12 — Gemini changes the system: recapture required (P0)

Useful sources:

- `captures/scene08_action_thinking.png` clearly shows the prior request;
- `captures/scene08_action_response_22.png` clearly shows the returned analysis ID and idempotent
  behavior;
- the trace visibly includes `request_field_analysis`;
- `raw/scene_08_agent_action.mp4` is a strong emergency fallback.

Why recapture:

- V2 explicitly promises `LIVE AGENT ACTION · UNINTERRUPTED`;
- the existing MP4 is generated from four still images;
- the approved request is shorter: `Compare this field again using three areas.`

Record one normal-speed take beginning before the prompt is entered and ending only after the queued
analysis ID and `request_field_analysis` trace are visible. Do not splice response states into this
scene.

### Scene 13 — feedback: recapture preferred (P1; fallback acceptable)

Useful sources:

- `captures/scene09_feedback_before.png`;
- `captures/scene09_feedback_after.png`;
- `raw/scene_09_feedback.mp4`.

The existing confirmation is real and legible, so it can be reused if schedule pressure prevents a
fresh click. A new take is preferable because the feedback should visibly follow the new natural
question and be associated with that exact analysis/conversation. If the prior analysis already has a
rating and disables another submission, use the newly created analysis/session rather than staging a
fake click.

### Scene 14 — Google Cloud production proof: recapture required (P0)

Useful supporting sources:

- `captures/scene10_cloud_services.png` confirms the four service names;
- `assets/scene_10_cloud_verified.svg` and `callout_06_cloud_roles.svg` can clarify the architecture
  after real Console proof.

Why recapture:

- the images are 960×698 and must be enlarged substantially for a 1080p master;
- the account avatar is visible and the UI is in Portuguese;
- one capture contains a promotional pop-up;
- there is no actual worker log with the demonstration analysis ID;
- the displayed/editorial revisions and image tag are older than the current deployment.

Capture a clean 1920×1080 Console view, with account identity cropped, showing all four current Cloud
Run services and their ready revisions. Then open a safe worker log filtered to the same analysis ID
from the newly recorded workflow/action. Do not show request headers, tokens, environment variables,
secrets, billing, email, or exact coordinates.

## Recommended V2 source layout

Preserve the genuine masters separately from the user-requested per-scene cuts:

```text
C:\Users\giova\Videos\1415-Agri-Hackathon\v2\
  source-masters\
    02_live_workflow_master.mp4      # native speed; form through completed result
    03_gemini_grounded_master.mp4    # exact natural question, answer, trace
    04_agent_action_master.mp4       # uninterrupted prompt through queued ID/trace
    05_feedback_master.mp4           # real rating and confirmation
    06_cloud_proof_master.mp4        # four services and matching safe log
  scenes\
    scene_08_field_boundary.mp4      # derived from 02, with no internal splice
    scene_09_satellite_workflow.mp4  # derived from 02, uniform 2×
    scene_10_season_priority.mp4     # derived from 02
    scene_11_farmer_question.mp4     # derived from 03
    scene_12_agent_action.mp4        # derived from 04, uncut
    scene_13_feedback.mp4            # derived from 05
    scene_14_cloud_proof.mp4         # derived from 06
```

The scene files are replaceable editing units. The untouched masters are the integrity evidence. Store
both, and record the new initial analysis ID, state-change analysis ID, recording time, duration,
resolution, browser language, and privacy review result in a local manifest without coordinates or
credentials.

## Minimum acceptance gate before V2 assembly

- Scene 8–10 master exists as one continuous native-speed file and reaches a completed real result.
- Guided Demo is visibly off; precise coordinates are not readable.
- One analysis ID is captured and can be matched to a safe worker log.
- The result shows five real dates, NDVI, NDRE, NDMI, cloud cover, bands and provenance.
- Scene 11 uses the approved natural question and returns grounded Area 1 evidence plus practical
  checks; Gemini 3.5 Flash, Google ADK, specialist and MCP/evidence tools are legible.
- Scene 12 is one uninterrupted take and visibly returns a new/replayed analysis ID plus
  `request_field_analysis`.
- Scene 13 visibly accepts a real rating, or the prior real confirmation is transparently used as a
  pickup.
- Scene 14 shows current Cloud Run revisions and a real safe log with the matching ID; no avatar,
  email, billing, secret, token, full coordinate, or unrelated tab is visible.
